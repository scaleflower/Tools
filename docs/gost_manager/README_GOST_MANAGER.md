# GOST 端口映射管理工具 使用说明

## 概述

这是一个交互式的GOST端口映射管理工具，支持管理多个GOST服务器的配置。

## 快速开始

```bash
cd /Users/heyake/Documents/Jacky/gost/scripts
python3 gost_manager.py
```

## 支持的GOST服务器

| 序号 | 名称 | API地址 |
|-----|------|---------|
| 1 | YTLC1 | http://192.168.123.239:8090 |
| 2 | YTLC2 | http://192.168.123.224:8090 |
| 3 | ECSHK | http://192.168.123.1:18080 |
| 4 | FSMTC | http://192.168.123.180:8090 |
| 5 | KTRN | http://192.168.123.163:18080 |

## 功能菜单

```
主菜单:
----------------------------------------
  1. 选择GOST服务器
  2. 查看所有服务
  3. 添加单个服务
  4. 删除单个服务
  5. 批量添加服务
  6. 删除所有服务 (需管理密码)
  7. 测试服务器连接
  8. 备份服务器配置
  0. 退出
----------------------------------------
```

## 功能详解

### 1. 选择GOST服务器

切换当前操作的GOST服务器。显示格式：

```
┌────┬──────────┬─────────────────────────────────┐
│ 序号│  名称    │          API地址               │
├────┼──────────┼─────────────────────────────────┤
│  1  │ YTLC1    │ http://192.168.123.239:8090    │
│  2  │ YTLC2    │ http://192.168.123.224:8090    │
│  3  │ ECSHK    │ http://192.168.123.1:18080     │
│  4  │ FSMTC    │ http://192.168.123.180:8090    │
│  5  │ KTRN     │ http://192.168.123.163:18080   │
└────┴──────────┴─────────────────────────────────┘
```

### 2. 查看所有服务

列出当前服务器上所有已配置的端口映射服务。

### 3. 添加单个服务

添加一个新的端口映射服务。**操作前会自动备份当前配置。**

**输入项：**
- 服务名称：唯一标识符，如 `ktrn-pr-ocs01-ssh`
- GOST监听端口：GOST服务器监听的端口，如 `52001`
- 目标IP地址：内网目标服务器IP，如 `192.168.20.140`
- 目标端口：目标服务端口，如 `22`

### 4. 删除单个服务

根据服务名称删除指定的端口映射。

### 5. 批量添加服务

批量添加多个端口映射服务。**操作前会自动备份当前配置。**

**输入格式（用空格分隔）：**
```
服务名称    GOST端口    目标IP    目标端口
```

**示例：**
```
ktrn-pr-ocs01-ssh 52001 192.168.20.140 22
ktrn-pr-app01-ssh 52011 192.168.20.152 22
my-db-forward     53000 192.168.10.100 3306
```

输入空行结束输入，然后确认批量添加。

### 6. 删除所有服务

删除当前服务器上的所有端口映射服务。

**⚠️ 危险操作，需要验证：**
1. 输入管理密码：`iWhale@2023`
2. 输入确认文本：`DELETE ALL`

### 7. 测试服务器连接

测试当前服务器的API连接是否正常。

### 8. 备份服务器配置

手动备份当前服务器的完整配置到本地。

## 备份说明

### 自动备份

以下操作会自动触发备份：
- 添加单个服务
- 批量添加服务

### 手动备份

通过菜单选项 `8. 备份服务器配置` 手动执行备份。

### 备份文件位置

```
/Users/heyake/Documents/Jacky/gost/scripts/backups/
```

### 备份文件格式

```
{服务器名}_{日期时间}.json
```

**示例：**
```
KTRN_20260403_150530.json
YTLC1_20260403_151000.json
```

### 备份文件内容

```json
{
  "backup_info": {
    "server_name": "KTRN",
    "server_url": "http://192.168.123.163:18080",
    "backup_time": "2026-04-03 15:05:30",
    "backup_by": "gost_manager"
  },
  "config": {
    "services": [...],
    "authers": [...],
    "chains": [...],
    ...
  }
}
```

## 服务配置格式

每个端口映射服务的配置格式：

```json
{
    "name": "ktrn-pr-ocs01-ssh",
    "addr": ":52001",
    "handler": {
        "type": "tcp"
    },
    "listener": {
        "type": "tcp"
    },
    "forwarder": {
        "nodes": [
            {
                "name": "ktrn-pr-ocs01",
                "addr": "192.168.20.140:22"
            }
        ]
    }
}
```

## 安全说明

1. **鉴权信息已内置**：API认证信息已配置在脚本中，无需手动输入
2. **危险操作保护**：删除所有服务需要输入管理密码
3. **自动备份**：添加服务前自动备份，可回滚

## 文件结构

```
/Users/heyake/Documents/Jacky/gost/scripts/
├── gost_manager.py          # 主程序
├── recreate_services.py     # 批量重建服务脚本
├── backup_gost_config.sh    # 备份脚本（旧）
└── backups/                 # 备份文件目录
    ├── KTRN_20260403_144439.json
    ├── YTLC1_20260403_144359.json
    └── ...
```

## 常见使用场景

### 场景1：批量添加新服务器端口映射

1. 选择GOST服务器
2. 批量添加服务
3. 按格式输入多个映射
4. 确认添加

### 场景2：误删恢复

1. 查看备份文件
2. 手动从备份JSON中提取服务配置
3. 重新添加服务

### 场景3：迁移配置到新服务器

1. 选择源服务器
2. 备份配置
3. 切换到目标服务器
4. 从备份文件中读取配置并添加

## 注意事项

1. 首次使用请先选择服务器
2. 批量添加前建议先手动备份一次
3. 删除操作不可恢复，请谨慎操作
4. 备份文件包含完整配置，可用于灾难恢复

## 依赖

- Python 3.x
- requests 库（`pip install requests`）

---

*文档更新日期：2026-04-03*
